<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.openrank.openrank.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.openrank.openrank.model.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="email" property="email"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <select id="findByUsername" resultMap="UserResultMap">
        SELECT id, username, password, email, created_at
        FROM user
        WHERE username = #{username}
        LIMIT 1
    </select>

    <insert id="insertUser" parameterType="com.openrank.openrank.model.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, email, created_at)
        VALUES (#{username}, #{password}, #{email}, #{createdAt})
    </insert>

</mapper>
